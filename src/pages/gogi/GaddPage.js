import React from "react";
import { useParams } from "react-router";

// 고깃집 리뷰 쓰기 페이지입니다.
const GaddPage = () => {
  // const { selectedTime, reserCount } = useParams();
  return (
    <div>
      <h2>고깃집 리뷰 쓰기</h2>
      {/* <p>{selectedTime}</p>
      <p>{reserCount}</p> */}
    </div>
  );
};

export default GaddPage;
